language: node_js

node_js:
  - 12.16.1

cache: npm

install: npm install

script: npm run test-with-coverage

deploy:
  skip_cleanup: true
  provider: pages
  github-token: $GITHUB_TOKEN
  on:
    branch: develop

